/**
 * 포트의 임시 더미 구현. <b>담당자의 실제 구현이 머지되면 삭제 대상이다.</b>
 * <p>
 * 전부 {@code @ConditionalOnMissingBean} 이라 같은 타입의 빈이 하나라도 등록되면 자동으로 물러난다.
 * 덕분에 앞단 1~4단계가 끝나기 전에도 리포트 생성 파이프라인 전체를 끝까지 돌려볼 수 있다.
 * <p>
 * 더미 값은 "그럴듯한 중간값"으로 골랐다 — 0 이나 null 로 채우면 프롬프트 분기(근거 없음 처리)만
 * 타게 되어 정작 정상 경로를 검증하지 못한다. 대신 멤버마다 같은 값이 나오지 않도록
 * projectMemberId 로 살짝 흔들어, 리포트에 전원 동일한 문장이 나오는 걸 막는다.
 */
package com.plog.domain.report.port.fake;
